name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  extension:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Package extension
        run: cd extension && zip -r ../LibrAA-Extension-${{ github.ref_name }}.zip .

      - name: Upload extension to release
        uses: softprops/action-gh-release@v2
        with:
          files: LibrAA-Extension-${{ github.ref_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  safari-extension:
    permissions:
      contents: write
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Build Safari extension
        run: |
          cd "safari-extension/LibrAA Translator"
          xcodebuild -scheme "LibrAA Translator" -configuration Release build

      - name: Package Safari extension
        run: |
          BUILD_DIR=$(xcodebuild -scheme "LibrAA Translator" -configuration Release -showBuildSettings -project "safari-extension/LibrAA Translator/LibrAA Translator.xcodeproj" 2>/dev/null | grep -m1 'BUILT_PRODUCTS_DIR' | awk '{print $3}')
          cd "$BUILD_DIR"
          zip -r "$GITHUB_WORKSPACE/LibrAA-Safari-Extension-${{ github.ref_name }}.zip" "LibrAA Translator.app"

      - name: Upload Safari extension to release
        uses: softprops/action-gh-release@v2
        with:
          files: LibrAA-Safari-Extension-${{ github.ref_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --target aarch64-apple-darwin
          - platform: macos-latest
            args: --target x86_64-apple-darwin
          - platform: windows-latest
            args: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Rust
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: npm install

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'LibrAA ${{ github.ref_name }}'
          releaseBody: |
            LibrAA ${{ github.ref_name }}

            ## Downloads
            - **macOS (Apple Silicon)**: `.dmg` (aarch64)
            - **macOS (Intel)**: `.dmg` (x86_64)
            - **Windows**: `.msi` / `.exe`
            - **Chrome Extension**: `.zip` (압축 해제 후 chrome://extensions에서 로드)
            - **Safari Extension**: `.zip` (앱 실행 후 Safari 설정에서 활성화)
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
